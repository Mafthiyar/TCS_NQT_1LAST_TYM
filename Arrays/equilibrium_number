import java.util.*;
class equilibrium{
        public static void main (String[]args){
            int arr[] = {1, 3, 5, 2, 2};
            int total = 0;

            for(int s : arr){
                total = total + s;
        }

            int left = 0;

            for(int i=0; i<arr.length; i++){
                total = total - arr[i];

                if(left == total){
                    System.out.println(" index : " + i + " idxed_number : " + arr[i]);
                }

                left = left + arr[i];
            }

    }
}
