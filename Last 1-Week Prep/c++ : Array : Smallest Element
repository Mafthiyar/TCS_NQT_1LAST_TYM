#include <iostream>
#include <array>
#include <algorithm>

int main(){
    std::array<int,4> arr = {4,7,8,2};
    int min = arr[0];
    
    int l = arr.size();
    
    for(int i=1; i<l; i++){
        if(arr[i] < min){
            min = arr[i];
        }
    }
    
    std::cout<< min << " ";
    return 0;
}
